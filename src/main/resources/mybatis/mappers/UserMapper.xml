<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.recipe.gola.mapper.UserMapper">

	<select id="userlist" resultType="UserDTO">
	<![CDATA[
		SELECT *
		FROM user
		ORDER BY regdate DESC
	]]>
	</select>
	
	<insert id="insertuser">
	<![CDATA[
		INSERT INTO wabang.user (userId, userPwd, userName, userBirth, userPhone, userEmail, userAddress, regdate)
		VALUES(#{userId}, #{userPwd}, #{userName}, #{userBirth}, #{userPhone}, #{userEmail}, #{userAddress}, now())
	]]>
	</insert>
	
</mapper>